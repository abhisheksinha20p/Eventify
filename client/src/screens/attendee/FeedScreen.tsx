import React from 'react';
import { View, Text } from 'react-native';

const FeedScreen = () => (
  <View className="flex-1 items-center justify-center bg-background">
    <Text className="text-white text-xl">Attendee Feed</Text>
  </View>
);

export default FeedScreen;
